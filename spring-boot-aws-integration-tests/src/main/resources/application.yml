spring:
  application:
    name: simple-application

cloud:
  aws:
    region:
      static: eu-central-1
    stack:
      auto: false

sqs:
  orderEventQueue: order-event-queue

s3:
  orderEventBucket: order-event-bucket
